# Android Broker Verification

Verified on 2026-08-02 against the official Google Maven metadata and resolved artifacts:

- LiteRT-LM Android latest/release: `0.15.0`
- API signatures inspected from the resolved `litertlm-android-0.15.0-api.jar`
- `Engine(EngineConfig)`, `initialize()`, `createConversation()`, text content extraction,
  and explicit `close()` are used directly
- foreground-service type: `specialUse`, API-guarded below Android 14
- socket host: fixed `127.0.0.1`; port: `8080`

Fresh successful gate:

```text
gradlew testDebugUnitTest lintDebug assembleDebug --no-daemon --console=plain
BUILD SUCCESSFUL in 13m 2s
55 actionable tasks: 53 executed, 2 up-to-date
```

The suite reported 17 tests, 0 failures, 0 errors, and 0 skipped. Lint reported 0 errors
and 4 warnings: one intentional AndroidX Core 1.18 compatibility pin because 1.19 requires
API 37/AGP 9.1, plus three stylistic suggestions to replace synchronous, success-checked
SharedPreferences commits with KTX helpers.

The final debug APK is 60,636,817 bytes with SHA-256
`039b7384cc428dc384e67a1555d9faa3a54a081f9510b02c9d91305e55e2633b`. It passed
`apksigner verify --verbose --print-certs` using APK Signature Scheme v2. The debug signer
certificate SHA-256 is
`fc48c0a5d456cb31a0e12a8f08c3b0c09b5d03acce290f44d82be3211ed0578f`.

Gradle lock state and SHA-256 verification metadata were generated by rerunning the same
unit, lint, and assembly tasks with `--write-locks --write-verification-metadata sha256`;
that command also completed successfully.

The committed Gradle wrapper JAR was regenerated by Gradle 8.14.5. Its SHA-256,
`7d3a4ac4de1c32b59bc6a4eb8ecb8e612ccd0cf1ae1e99f66902da64df296172`, matches the
official Gradle 8.14.5 wrapper checksum.

Emulator runtime remains unverified. The installed `CompText_Begleiter_API_35` AVD and a
separate temporary API 35 QA AVD both failed before ADB registration because the emulator
required a 7.37 GB/6.44 GB userdata partition while only about 3.05 GB was available. The
existing AVD was not wiped or modified. No APK install, UI frame, endpoint call, or logcat
claim is made from this environment.
