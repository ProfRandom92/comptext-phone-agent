# CompText Phone Agent 0.6.1 Release Evidence

## Target

- Repository: `ProfRandom92/comptext-phone-agent`
- Default branch: `main`
- Target commit: `33702f2ee4133221ffec0d3691508a983481482b`
- Version in `pyproject.toml`: `0.6.1`
- Python requirement: `>=3.12`
- License metadata and repository license: MIT

## Workflows

The repository contains dedicated workflows:

- `.github/workflows/python-ci.yml`
- `.github/workflows/android-ci.yml`
- `.github/workflows/security-ci.yml`
- `.github/workflows/release.yml`

Known prior evidence supplied for the target commit states that Python CI and Security CI succeeded. The combined-status connector endpoint returned no status objects, so this document does not claim additional check results beyond that known evidence. Android and release workflow results must be reviewed directly before publication.

## Release history immediately before target

- `5459b4aa30ada8ae5f4dbb6bed1884b222b14366` — packaged rollback verification hardening.
- `60feda73cda92fd6e9b9cb4ff642a67c3aa51de3` — terminal-independent CLI option assertions.
- `33702f2ee4133221ffec0d3691508a983481482b` — secondary Click option-name compatibility.

The two commits after `5459b4aa…` are test-portability changes and do not change runtime functionality.

## Verified repository constraints

- Package name: `comptext-phone-agent`
- Console scripts: `comptext-phone`, `comptext-chat`
- Core dependency set is pinned or bounded for Termux portability.
- Pydantic remains on the pure-Python 1.x line.
- The Android broker is optional and separate from the Python package.
- Release packaging and source-audit scripts are present under `scripts/`.

## Publication gates still open

- confirm Android CI result for the exact target commit;
- confirm release workflow dry-run or artifact build;
- verify all packaged filenames;
- calculate and compare SHA-256 checksums;
- run first-user sandbox smoke test from the final package;
- verify default uninstall data preservation and packaged rollback;
- inspect final Draft Release assets and notes;
- publish only after explicit authorization.